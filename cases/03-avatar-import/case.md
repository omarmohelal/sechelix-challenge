# Case 03 — avatar import

`import_avatar` is reachable from an authenticated endpoint where the user pastes
a URL to their profile picture. The service runs inside a VPC that also hosts an
internal metadata endpoint and several private services.

Is there a security defect in this file? If so, name the class, the boundary
that fails, and the smallest fix.
